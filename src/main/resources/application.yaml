spring:
  profiles:
    active: local # default

# local
---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sports?useSSL=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: Qwer1234!

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080